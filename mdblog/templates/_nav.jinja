<nav>
    <ul class="nav">
        <li>
            <a href="{{ url_for("view_about") }}" title="About">About</a>
        </li>
        <li>
            <a href="{{ url_for("view_articles") }}" litle="Articles">Articles</a>
        </li>
        <li>
            <a href="{{ url_for("view_admin") }}" title="Admin">Admin</a>
        </li>
        {% if "logged" in session %}
            <li class="li">
                <form action="{{ url_for("logout_user") }}" method="post">
                    <input type="submit" value="logout">
                </form>
        </li>
        {% endif %}
    </ul>
</nav>